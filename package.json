{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/hub",
    "test": "npm run test -w packages/hub",
    "dev": "npm run dev -w packages/hub"
  }
}
